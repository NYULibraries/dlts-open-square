# Open Square Data Refresher

hugo --ignoreCache

### Commands
To refresh the markdown files, use the `hugo` command with the cache ignored and the appropriate environment:

```sh
# For staging
hugo --ignoreCache -e stage

# For production
hugo --ignoreCache -e production
